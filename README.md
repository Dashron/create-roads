# create-roads
An initalizer package for a roads project (replaces roads starter)



# TODO
1. This should have all the ts and es config from roads and roads api
2. This should include roads and have a folder structure similar to the roads example. Make sure to include a static dir, public and private routes, and example routes/templates
3. This should be able to compile the public aspects of the project into client side javascript and serve the static files
4. This should include a config library and a simple (popular) logger
5. This should have a simple, tested, single index file to run the roads project
6. This should set up a roads API system and include the middleware so that it can be accessed by roads
7. This should allow you to, via a simple config change, toggle between a directly in-app connected road/road-api and a network-divided road/road-api
8. This should have a great developer experience, ensuring we can have dynamic file reloads in the browser and automatic server restarting on file change
9. This should have a single command that makes it easy to create the scaffolding for a new part of the project (e.g. users, blog posts, etc)
10. This should have csrf protection
11. This should have the awesome form validation stuff from starter and dd
12. This should have some of the common api and web routes and middleware from dd
13. This should not have any authentication, it should be an external easily attached project (maybe? a basic framework might be nice)
14. This should not have a user module, it should be an external easily attached project
15. This *maybe* should have a easy docker config included.